import { FiltroPorTextoPipe } from './filtro-por-texto.pipe';

describe('FiltroPorTextoPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPorTextoPipe();
    expect(pipe).toBeTruthy();
  });
});
