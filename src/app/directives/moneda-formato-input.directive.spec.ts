import { MonedaFormatoInputDirective } from './moneda-formato-input.directive';

describe('MonedaFormatoInputDirective', () => {
  it('should create an instance', () => {
    const directive = new MonedaFormatoInputDirective();
    expect(directive).toBeTruthy();
  });
});
